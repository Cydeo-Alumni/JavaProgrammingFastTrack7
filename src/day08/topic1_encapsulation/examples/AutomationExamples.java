package day08.topic1_encapsulation.examples;

public class AutomationExamples {

    /*
    Driver
        private static WebDriver driver;
        getDriver()

     --------------------------------------------
     Config Reader
        private static Properties properties = new Properties();
        getProperty(String)

     --------------------------------------------
     FindBy
            @FindBy (css = "#username")
            public WebElement userName;

            grouping the methods that find the locator with the object we are able to access in the page

    --------------------------------------------
    JSON files
        @Data
        @JsonIgnoreProperties(ignoreUnknown = true)
        public class Student{
            private String firstName;
            private int batch;
        }
         lombok allowed getters and setters to be generated

     */

}
